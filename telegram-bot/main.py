

async def main() -> None:
    ...
    # for initializete tables
    # await create_tables()
    # if not BOT_TOKEN: logger.exception('Bot Token not found')
    #     raise ValueError('BOT_TOKEN not found')
    # else:
    #     logger.debug('Bot Token: %s', BOT_TOKEN)
    #     bot = Bot(
    #         BOT_TOKEN, default=DefaultBotProperties(parse_mode=ParseMode.HTML)
    #     )
    #     await dp.start_polling(bot)
